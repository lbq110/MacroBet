.env.example 是一个模板文件，用于记录所需的环境变量。

# .env.example - Copy this to .env and fill in the values

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=your_password
DB_NAME=macrobet

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Server
PORT=3000

# CORS (set to your frontend URL in production)
CORS_ORIGIN=*
